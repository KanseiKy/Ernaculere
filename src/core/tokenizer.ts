import type { Ernaculere } from '../deps.ts';

export function tokenizer(source: string): Array<Ernaculere.Token> {
    const tokens: Array<Ernaculere.Token> = [];
    let pointer = 0;

    return tokens;
}